#include <stdio.h>
#include <conio.h>

int searching(int *x,int n,int key)
{
    int i;
    for(i=0; i<n; i++)
    {
        if(x[i]==k in the list ");
}
int main()
{
    int x[10],n,key,i;
    printf("\n Enter the no of array element");
    scanf("%d",&n);
    for(i=0key element :- ");
    scanf("%d",&key);
    searching(x,n,key);
    getch();
}